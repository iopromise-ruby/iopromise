# frozen_string_literal: true

RSpec.describe IOPromise do
  it "has a version number" do
    expect(IOPromise::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
